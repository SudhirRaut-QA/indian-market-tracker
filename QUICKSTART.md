"""
Quick Start Guide
==================

Run once:
  python -m tracker --now

Full analysis:
  python -m tracker --now --full

Start scheduler (8 daily times):
  python -m tracker --schedule

Setup check:
  python -m tracker --setup
"""
